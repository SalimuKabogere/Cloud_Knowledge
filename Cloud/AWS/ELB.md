This is the Elastic Load Balance
	 It distributes network traffic evenly across instances
	 It is called Elastic because it has the ability to scale up and down basing on demand
It acts as  a single point of contact, spreading requests evenly so no single instance gets overloaded.

How the traffic is distributed across the instances
>>Round  Robin
>>Least Connections
>>IP Hash
>>Least Response Time
